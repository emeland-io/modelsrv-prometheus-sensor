// Package prometheus wraps the Prometheus HTTP API for instant queries that
// reduce to a single scalar value. It targets a configurable Prometheus base
// URL and performs no authentication (first version).
package prometheus
